# Reverse Mapping

The reverse mapping is written as:

\[
g: P \rightarrow \hat{D}
\]

It represents the attempt to reconstruct an approximate dynamical representation \(\hat{D}\) from psychological descriptions \(P\).

The hypothesis predicts that this reconstruction is lossy because many different dynamical states or trajectories may produce similar psychological descriptions.
